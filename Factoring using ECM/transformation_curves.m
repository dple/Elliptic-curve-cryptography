// Transform between various curves
// Weierstrass curves <-> Montgomery curves
// Edwards curves <-> Montgomery curves
// Edwards curves <-> Weierstrass curves
// Weierstrass form: y^2 = x^3 + a*x + b
// Montgomery form: B*y^2 = x^3 + A*x^2 + x
// Twisted Edwards curves: a*x^2 + y^2 = 1 + d*x^2*y^2

WeierToMont := function(x, y, a, b)
	local A, B, u, v, l;
	l := 
end function;

